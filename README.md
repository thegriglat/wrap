# wrap
Wrap stdin in columns

## Usage
```
$ wrap  --help
Wrap stdin in columns

Usage: wrap [OPTIONS]

Options:
  -w, --wrap <WRAP>  Column length [default: 80]
  -h, --help         Print help
  -V, --version      Print version
 ```
